#include <stdio.h>

int main()
{
	int numero, i, j, letra;
	char letra[50];
	
	scanf("%d", &numero);
	
	for(i=0; numero <= 25; i++) {
		letra++;
		scanf("%s", &letra);
		
		for(j=0; numer)
	}

	return 0;
}

